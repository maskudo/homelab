apiVersion: v1
kind: Service
metadata:
  labels:
    app: immich
  name: immich
spec:
  ports:
    - name: immich
      port: 2283
      protocol: TCP
      targetPort: 2283
  selector:
    app: immich
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: immich-redis
  name: immich-redis
spec:
  ports:
    - name: immich-redis
      port: 6379
      protocol: TCP
      targetPort: 6379
  selector:
    app: immich-redis
  type: ClusterIP

---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: immich-postgres
  name: immich-postgres
spec:
  ports:
    - name: immich-postgres
      port: 5432
      protocol: TCP
      targetPort: 5432
  selector:
    app: immich-postgres
  type: ClusterIP
